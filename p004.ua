# 0.9.5
◴♭⊞(×).⇡1000 # create an array of every product of two numbers from 1 to 999
∵⇌.°⋕⊏⍏.     # transform every number to a string
▽=1:⊙=.:     # filter out all every number that are not palyndromes by comparing them to their reverse
⊢⇌           # reverse the array to get the higher numbers first, then take the first
⋕°□          # unbox to get the number
